<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="20dp"
    android:background="#C6DBE2">

    <!-- Library Card -->
    <androidx.cardview.widget.CardView
        android:id="@+id/cardLibrary"
        android:layout_width="match_parent"
        android:layout_height="80dp"
        android:layout_marginBottom="20dp"
        card_view:cardCornerRadius="16dp"
        card_view:cardElevation="6dp"
        android:clickable="true"
        card_view:cardBackgroundColor="@color/medium_blue"
        android:focusable="true">

        <TextView
            android:layout_width="150dp"
            android:layout_height="match_parent"
            android:layout_marginLeft="100dp"
            android:drawableLeft="@drawable/library_logo"
            android:drawableTint="@color/white"
            android:gravity="center"
            android:text="Library"
            android:textColor="@color/white"
            android:textSize="20sp"
            android:textStyle="bold"

            />
    </androidx.cardview.widget.CardView>

    <!-- Hostel Card -->
    <androidx.cardview.widget.CardView
        android:id="@+id/cardHostel"
        android:layout_width="match_parent"
        android:layout_height="80dp"
        card_view:cardBackgroundColor="@color/medium_blue"
        card_view:cardCornerRadius="16dp"
        card_view:cardElevation="6dp"
        android:clickable="true"
        android:focusable="true">

        <TextView
            android:layout_width="150dp"
            android:layout_height="match_parent"
            android:text="Hostel"
            android:textSize="20sp"
            android:textStyle="bold"
            android:textColor="@color/white"
            android:gravity="center"
            android:layout_marginLeft="100dp"
            android:drawableLeft="@drawable/hotel_logo"
            android:drawableTint="@color/white"/>
    </androidx.cardview.widget.CardView>

</LinearLayout>